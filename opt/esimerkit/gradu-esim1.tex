%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
%       Esimerkki gradu.cls -tyylitiedoston k‰ytˆst‰
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
\documentclass[a4paper,12pt]{gradu}
\usepackage[T1]{fontenc}
\usepackage[finnish]{babel}
\usepackage{palatcm}     %  Palatino/Computer modern fontti
\usepackage{amsmath}     %  AmS-LaTeX konstruktiot (align, aligned,...)
\usepackage{amsfonts}    %  AmS-LaTeX fontit
\usepackage{graphicx}    %  EPS-tiedostojen ottaminen mukaan, PS-temput,...
%



\begin{document}

%***********************
%     Kansilehti 
%***********************
\thispagestyle{empty}
\vspace*{7truecm}

\centerline{\bf Tiina Tutkija}

\vspace{2truecm}

\centerline{\Large\bf Pro gradun kirjoittamisesta LaTeX-ohjelmalla}

\vspace{4truecm}

{\parindent9truecm\parskip0pt
        Tietotekniikan (tietoliikenne)\par
        pro gradu -tutkielma\par
        3.5.2000
}

\vfill

\centerline{\large\bf Jyv‰skyl‰n yliopisto}
\centerline{\bf Tietotekniikan laitos}



\newpage

%***********************
%   sis‰llysluettelo
%***********************

\tableofcontents     

\newpage

%***********************
%     tiivistelm‰
%***********************

\subsection*{Tiivistelm‰}

T‰m‰n tekstin tarkoituksena on esitell‰ gradu.cls tyylitiedoston 
k‰yttˆ‰ prog gradu -tutkielman tekemisess‰. Teksti sis‰lt‰‰ 
sielt‰ t‰‰lt‰ napattuja tekstinp‰tki‰,
eik‰ siis mit‰‰n varsinaista asiaa!

\bigskip

{\bf Title in english:} Writing MSc thesis using LaTeX.

\newpage

\section{Johdanto}

\TeX on Donald E. Knuthin kehitt‰m‰ ladontaohjelma, joka soveltuu erityisesti
matemaattisen tekstin tuottamiseen. \LaTeX-makrokirjasto sis‰lt‰‰ monia
sellaisia piirteit‰, joita paljas \TeX ei sis‰ll‰. N‰it‰ ovat esimerkiksi
sis‰llysluettelo, lukujen ja kaavojen automaattinen numerointi ja symbolinen
viittaus niihin,...

{\tt gradu.cls} -tyylitiedostossa asetetaan oikeankokoiset marginaalit
ja m‰‰ritell‰‰n ymp‰ristˆt {\tt maaritelma}, 
{\tt lause}, {\tt lemma},
{\tt todistus}, {\tt esimerkki} ja {\tt algoritmi}.






\def\nonz{\operatorname{Nonz}}
\def\Fill{\operatorname{Fill}}
\def\tran#1{{#1}^T}

\section{Symmetrisen matriisin symbolinen kolmiointi}

\subsection{T‰yttyminen} 

Er‰s mahdollinen valinta painofunktioksi on
%
\begin{equation}
  w_i(x) = |x-x_i|^{-\mu}, \quad \mu>0.
  \label{painofun-kaava}
\end{equation}
%
Edellinen painofunktio \eqref{painofun-kaava} ottaa huomioon kaikki
interpolaatiopisteet $x_i$, joten tyˆm‰‰r‰ kasvaa suureksi.

Olkoon 
%
\begin{equation}
  \nonz(A) = \{(i,j)\mid a_{ij}\ne0,\ i\ne j\}. 
\end{equation}
%
Olkoon $A$:lla kolmiohajotelma
$A=L\tran L$. Asetetaan $F=L+\tran L$. 
Alkiot $a_{ij}$ saadaan aina lausekkeista,
miss‰ esiintyy $a_{ij}$ ja jotain muuta. Jos $a_{ij}\ne0$, tulee vastaava
tekij‰n alkio nollaksi vain sattumalta. T‰ten useimmiten
%
\begin{equation*}
 \nonz(A) \subseteq \nonz(F).
\end{equation*}
%
M‰‰ritell‰‰n 
%
\begin{equation}
  \Fill(A) = \nonz(F) \setminus \nonz(A).
\end{equation}
%
T‰m‰ k‰sitt‰‰ siis ne
kolmiotekijˆiden nollasta eroavat alkiot, joita vastaavissa kohdissa
alkuper‰isess‰ matriisissa $A$ on nollat.

Jos tiedet‰‰n etuk‰teen mitk‰ $L$:n alkiot ovat $\ne0$, voidaan k‰ytt‰‰
matriisille $A$ tiivist‰ listaesityst‰. Tilaa varataan vain 
alkioille $l_{ij}:\ (i,j)\in\nonz(A)\cup\Fill(A),\ i\ge j$ ja tarvittaville
indekseille ja linkeille. 
%
\begin{equation}
  u(x) = \int_{\sqrt2}^{\sqrt{7}} \frac{x+\sin x}{1+x^2+\eta+\zeta}\,dx
   \label{int-kaava}
\end{equation}
%
Toimenpiteit‰, joilla selvitet‰‰n $\Fill(A)$,  kaavan \eqref{int-kaava}
syvin olemus ja
muodostetaan tarvittavat tietorakenteet $L$:n tallettamiseksi, sanotaan
matriisin {\em symboliseksi kolmioinniksi}.




Jotta symbolisesta kolmioinnista olisi hyˆty‰, tulee
$\Fill(A)$ pit‰‰ mahdollisimman pienen‰. Osoittautuu, ett‰ muuttujien
numeroinnilla voidaan siihen vaikuttaa suuresti.

\subsection{Eliminointigraafit} 

Tarkastellaan, miten sarakemenetelm‰ss‰
nollasta eroavien alkioiden m‰‰r‰ lis‰‰ntyy. Sarakkeesta $k$
v‰hennet‰‰n sarakkeen $j$ loppuosa kerrottuna $\overline{a_{kj}}$:lla.
Sarakkeessa $k$ on sen j‰lkeen nollasta eroava alkio $a_{ik}$, jos
sellainen oli jo ennest‰‰n, tai $a_{kj}\ne0$ ja $a_{ij}\ne0$.

Olkoon $H^{(j)}$ symmetrinen osamatriisi, kun saraketta $j$ k‰sitell‰‰n.
Tarkastellaan tilannetta matriisin $H^{(j)}$ graafin avulla:
Todetaan, ett‰ $H^{(j)}$:n ensimm‰isess‰ sarakkeessa
$v^{(j)}$ tietty komponentti $v^{(j)}_k\ne0$
tarkalleen silloin, kun vastaavassa graafissa solmut $k$ ja $j$
ovat naapureita. Matriisissa $H^{(j+1)}$ taas solmut $k$ ja $i$
tulevat naapureiksi silloin, kun ne olivat naapureita jo graafissa
$H^{(j)}$ tai ne molemmat olivat solmun $j$ naapureita. T‰ten matriisia
$H^{(j+1)}$ vastaava graafi saadaan matriisin $H^{(j)}$ graafista
seuraavasti:

\begin{itemize}
\item poistetaan solmu $j$ ja siit‰ sen naapureihin johtavat tiet
\item kaikki solmun $j$ naapurit graafissa $H^{(j)}$ yhdistet‰‰n
 pareittain teill‰, mik‰li niiden v‰lill‰ ei ollut jo ennest‰‰n tiet‰.
\end{itemize}

\begin{esimerkki}\label{symfac-esim-1} 
 Tarkastellaan
kuvan~\ref{kuva-1} matriisia $H^{(0)}:=A$. Eliminoidaan 
muuttuja $x_1$. Alkuper‰ist‰
matriisia vastaavassa graafissa $G_0$ solmun 1 naapurit ovat 2 ja 6.
Poistetaan solmu 1 ja siit‰ johtavat tiet. Yhdistet‰‰n solmut 2 ja
6 tiell‰, joka matriisissa vastaa sit‰, ett‰ alkio $a_{26}$ ja
$a_{62}$ tulevat nollasta eroaviksi.


Eukleideen algoritmi voidaan toteuttaa Fortran90-kielell‰ seuraavasti:
\begin{verbatim}
program GCD
  integer:: m, n, r
  do
    write(*,*) "Please give values for m and n"
    read(*,*) m, n
    do while ( n .ne. 0 )
       r = mod( m, n )
       m = n
       n = r
    end do
    write(*,*) "gcd = ", m
  end do
end program GCD
\end{verbatim}
%
Lis‰‰ kielest‰ voi opiskella kirjoista \cite{Counihan}, \cite{CSC90}.


Sen j‰lkeen elminoidaan muuttuja $x_2$. Graafissa sen naapurit ovat
3,4 ja 6. Solmu 2 poistetaan ja solmut 3,4, ja 6 yhdistet‰‰n teill‰
jos niit‰ ei ennest‰‰n ole. T‰ss‰ tapauksessa ei ole, joten
alakolmioon tulee kolme uutta nollasta eroavaa alkiota. Matriisit
$H^{(j)}$ ja niit‰ vastaavat graafit on esitetty kuvassa~\ref{kuva-1}.
\end{esimerkki}





\subsection{Minimiastemenetelm‰ hyv‰n permutaation m‰‰r‰‰miseksi}

Tavoitteena on saada sellainen muuttujien permutaatio, ett‰ 
lopullisessa tekij‰matriisissa $L$ olisi mahdollisimman v‰h‰n
nollasta eroavia alkioita.

Olkoon $G^F$ graafi, joka vastaa matriisin $F=L+\tran L$ rakennetta.
T‰m‰ saadaan lis‰‰m‰ll‰ alkuper‰iseen graafiin ne tiet, jotka
lis‰ttiin eliminointigraafeihin eri vaiheissa. N‰iden lukum‰‰r‰
olisi pidett‰v‰ mahdollisimman pienen‰ valitsemalla solmujen
j‰rjestys sopivasti. Toistaiseksi ei tiedet‰ voidaanko t‰m‰
probleema ratkaista polynomialisessa ajassa. Siksi k‰ytet‰‰n
heuristisia menetelmi‰.




\subsection{Tekij‰graafien k‰yttˆ symbolisessa kolmioinnissa} 

Olkoot
$E^A$ ja $E^F$ matriiseja $A$ ja $F=L+\tran L$ vastaavien graafien teiden
joukot.
Seuraava lause antaa $L$:n nollasta eroavien alkioiden
paikat:

\begin{lause} Suoritetaan eliminointi j‰rjestyksess‰ $x_1,x_2,...,x_n$.
Tie $(i,j)$ kuuluu joukkoon $E^F$ silloin ja vain silloin, kun se
kuuluu joukkoon $E^A$ tai on olemassa $k<\min\{i,j\}$ siten, ett‰
sek‰ tie $(k,i)$ ett‰ tie $(k,j)$ kuuluvat joukkoon $E^F$.
\end{lause}



\def\Reach{\operatorname{Reach}}

Olkoon $S$ jokin solmujoukko suuntaamattomassa graafissa. 
Solmun $i$ {\it S-naapureiden joukko} $\Reach(i,S)$ m‰‰ritell‰‰n seuraavasti:
Solmu $j$ on solmun $i$ S-naapuri ($i,j\notin S$), jos $j$ on $i$:n tavallinen
naapurisolmu, tai jos on olemassa matriisi
%
\begin{equation}
   C = \bmatrix 
           \sqrt2  &\frac13  &\frac56 \\
            0   &12 &\pi \\
            45 &\frac19 &\sqrt3
       \endbmatrix
\end{equation}
%
miss‰ jokainen solmuista $s_1,...,s_m$ kuuluu joukkoon
$S$.

\begin{lause} Suoritetaan eliminointi j‰rjestyksess‰ 
$x_1,x_2,...,x_n$. 
Silloin solmun $i$ naapurit graafissa $G^F$ ovat $=\Reach(i,S_{i-1})$,
miss‰ $S_{i-1}$ on solmujoukko $1,...,i-1$. T‰ss‰ Reach-operaatio
suoritetaan graafissa $G^A$.
\end{lause}

\begin{figure}
 \begin{center}
 {\tt\Huge :-)    )-:}
 \end{center}
 \caption{Teatteri} \label{kuva-1}
\end{figure}




Olkoon $P_1,P_2,...,P_m$ jokin
graafin $G$ solmujen partitio. Muodostetaan uusi graafi, jonka
solmut ovat $P_1,...,P_m$. Solmujen $P_i$ ja $P_j$ v‰lill‰ on tie
tarkalleen silloin, kun ainakin yhdest‰ joukon $P_i$ solmusta
johtaa tie johonkin joukon $P_j$ solmuun alkuper‰isess‰ graafissa
$G$.




\begin{huomautus}
Koska $\bar S$ koostuu aina erillisist‰ solmuista, voidaan
joukko $\Reach(i,\bar S)$ m‰‰r‰t‰ yksinkertaisesti. Ensin kaikki
ne $i$:n naapurit, jotka eiv‰t kuulu joukkoon $\bar S$, otetaan
mukaan. Olkoon sitten $P(i)$ niiden solmun $i$ naapureiden
joukko, jotka kuuluvat joukkoon $\bar S$. Jokaisella solmulla
$p\in P(i)$ otetaan mukaan viel‰ ne $p$:n naapurit, mitk‰ eiv‰t
jo ennest‰‰n ole mukana. Huomattakoon, ett‰ mik‰‰n $\bar S$:n solmu
ei voi olla $P(i)$:n solmujen naapurina. T‰ten hakua ei tarvitse
laajentaa solmujen $P(i)$ ulkopuolelle.
\end{huomautus}




\begin{algoritmi}  Symbolinen kolmiointi tekij‰graafia k‰ytt‰en
\begin{enumerate}
 \item  Olkoon $T$ vuorossa oleva tekij‰graafi. Asetetaan
  aluksi $T:=G^A$, miss‰ $G^A$ on matriisia $A$ vastaava graafi.
  $\bar S:=\emptyset$. M‰‰r‰t‰‰n solmujen asteluvut.
 \item  Olkoon $i$ se $T$:n solmu $\notin\bar S$, jonka
   t‰m‰nhetkinen asteluku on pienin. Solmun asteluku$:=|\Reach(i,\bar S)|$.
 \item  Muuttuja $x_i$ on seuraava eliminointij‰rjestyksess‰.
  Vastaavan matriisin $L$ sarakkeen nollasta eroavien rivien
  numerot saadaan joukosta $\Reach(i,\bar S)$.
 \item  Solmu $i$ liitet‰‰n joukkoon $\bar S$. Jos mik‰‰n $i$:n
  naapureista ei kuulunut joukkoon $\bar S$, tulee $i$:st‰ $\bar S$:n
yksin‰inen solmu. Jos $i$:ll‰ on yksi tai useampia joukossa
 $\bar S$ olevia naapureita, sulautetaan ne yhdeksi $\bar S$:n
 solmuksi.
 \item  Kohdassa 3 muodostetun joukon $\Reach(i,\bar S)$ solmujen 
  naapurilistat muutetaan uutta tekij‰graafia vastaaviksi,
  mik‰li edell‰ sulautettiin solmuja yhteen. Joka tapauksessa
 m‰‰r‰t‰‰n uudelleen em.\ joukon solmujen asteluvut ja muutetaan
 astelukuihin perustuvaa solmujen $(\notin S)$ prioriteettijonoa
 vastaavasti. Toistetaan kohdasta 2 mik‰li viel‰ on j‰ljell‰ 
 solmuja, jotka eiv‰t kuulu joukkoon $\bar S$. Muuten lopetetaan.
 Silloin solmujen $i$ k‰yntij‰rjestys antaa minimiastemieless‰
 m‰‰r‰tyn permutaation.
\end{enumerate}
\end{algoritmi}









Realistisen n‰kym‰n aikaansaamiseksi k‰ytet‰‰n perspektiiviprojektiota.
T‰m‰ voidaan tehd‰ seuraavasti:
%
\begin{verbatim}
 ...
 glViewport( 0, 0, w, h );
 glMatrixMode( GL_PROJECTION );
 glLoadIdentity();
 gluPerspective( 45.0, w/h, 1.0, 3*z );
 ...
\end{verbatim}
T‰ss‰ $z=\max(w,h)$...


\section{Langaton tiedonsiirto}

Langaton tiedonsiirto on tullut suuren yleisˆn tietoisuuteen GSM-j‰rjestelm‰n
myˆt‰.
Kaksi p‰‰teknologiaa ovat TDMA ja CDMA. 


\subsection{Onko kuu juustoa?}

Nollasta poikkeava luku $x$ esitet‰‰n IEEE-aritmetiikan mukaisessa 
liukulukuesityksess‰ muodossa
%
\begin{equation}
  x = (-1)^s M\cdot 2^E.
\end{equation}
%
Kuva~\ref{IEEE-kuva} havainnollistaa liukuluvun bin‰‰rist‰ talletusmuotoa
tietokoneessa.

\begin{figure}
   \begin{center}
      \fbox{\quad $s$\phantom{$M$}}\fbox{\quad $M$ \quad}\fbox{\quad $E$\quad}
   \end{center}


   \caption{Liukuluvun talletusmuoto tietokoneessa.}% $M$ on liukuluvun
%     mantissa, $E$ eksponentti ja $s$ kertoo luvun merkin.}
     \label{IEEE-kuva}
\end{figure}

IEEE-aritmetiikka tuntee myˆs aritmeettisten erikoistapausten symbolit
{\tt Inf} (‰‰retˆn) ja {\tt NaN} (not-a-number).

\begin{maaritelma}
 Jos ahne algoritmi antaa joukkosysteemiss‰ $I$ aina optimiratkaisun
e.m.\ optimointiprobleemalle, niin $I$ on matroidi.
\end{maaritelma}

\subsection{Parametrisoidut splinit}

\begin{lemma} 
Jos solmulla ei ole olemassa mit‰‰n sellaista lyhint‰ reitti‰, miss‰
olisi enemm‰n kuin $k$ tiet‰, m‰‰r‰‰ algoritmi lyhimm‰n reitin
 $k$ ensimm‰isen kierroksen kuluessa.
\end{lemma}

\begin{todistus}
 Todetaan, ett‰ lemma on tosi kun $k=1$. Alkusolmun naapureihin saadaan
lyhimm‰t reitit selville jo ensimm‰isell‰ kierroksella, mik‰li niiss‰
ei voi olla enemp‰‰ kuin yksi tie. Induktio-oletus: Lemma on tosi arvoon
$k=h$ saakka...

... jolloin viimeist‰‰n $h+1$.\ kierroksella kiinnitet‰‰n lyhin reitti
solmuun $I$.
\end{todistus}






Tarkastellaan ohjelmaa, jonka skalaariversion kuluttama CPU-aika
on $T$ ja siit‰ osaa $S$ kuluu skalaarikodissa ja $V$ koodissa, joka
voidaan vektoroida. Jos vektorointi nopeuttaa suoritusta $n$-kertaisesti,
on vektoroidun ohjelman suoritukseen kuluva aika $S+V/n$, joten
saavutettava nopeuden lis‰ys on
%
\begin{equation*}
  s = \frac{S+V}{S+V/n}.
\end{equation*}
%
T‰m‰ tunnetaan {\em Amdahlin lakina}. Kuva~\ref{vect-kuva} esitt‰‰ 
suorituskyvyn  lis‰yst‰ vektorikoodin osuuden funktiona.

\begin{figure}
 \begin{center}
   \unitlength=0.5truecm
   \begin{picture}(10,10)
      \put(1,1){\line(1,0){9}}
      \put(1,1){\line(0,1){9}}
      \qbezier(1,2)(5,2)(9,8)
   \end{picture}
 \end{center}
 \caption{Suorituskyvyn lis‰ys vektorikoodin osuuden funktiona.}
    \label{vect-kuva}
\end{figure}



\begin{algoritmi} (Kaps‰kkiprobleeman LP-relaksaatio)
 \begin{enumerate}
  \item Asetetaan $N_0:=\emptyset,\ N_1:=\emptyset$, $N_F:=$kaikki muutttujat,
         $\bar a_0:=a_0$.
  \item Valitaan jokin suhdeluku $q:=c_i/a_i$. Suoritetaan joukon $N_F$
         ositus seuraaviin osajoukkoihin:
     \begin{align*}
       &NS:=\{ i\in N_F \mid q_i>q\} \\
       &NY:=\{ i\in N_F \mid q_i=q \}.
     \end{align*}
    Edelleen
      \begin{align*}
        &S_1(q):=\sum\{a_i \mid i\in NS\} \\
        &S_2(q):=S_1(q)+\sum\{a_i\mid i\in NY\}
      \end{align*}
   \item Jos $S_1(q)<\bar a_0\le S_2(q)$, lopetetaan. LP-relaksaation
      optimiratkaisu saadaan asettamalla....
   \item Jos joukossa $N_F$ on enemm‰in kuin $theta$ muuttujaa, toistetaan
      askeleesta 2. Muuten ratkaistaan muuttujien $N_F$ m‰‰r‰‰m‰
      kaps‰kkiprobleema j‰rjest‰m‰ll‰ n‰m‰ muutttujat suhteen $c_i/a_i$
      mukaiseen v‰henev‰‰n j‰rjestykseen...
 \end{enumerate}
\end{algoritmi}

\section{Globaali optimointi}

Ep‰lineaaristen optimointiteht‰vien ratkaisualgoritmit lˆyt‰v‰t
lokaalin minimin, eik‰ globaalisuutta yleens‰ voida osoittaa, elleiv‰t
kohdefunktio ja k‰yp‰ alue ole konvekseja. Yleens‰...

\subsection{Geneettiset algoritmit}

Geneettiset algoritmit ovat luonnon evoluutiomekanismeja matkivia 
optimointimenetelmi‰. GA-menetelm‰t eiv‰t vaadi kohdefunktion jatkuvuutta,
joten niit‰ voidaan k‰ytt‰‰ perinteisill‰ menetelmill‰ vaikeasti
ratkaistaviin ongelmiin.

\begin{huomautus} 
 I/O:ta voidaan tehd‰ myˆs samanaikaisesti ohjelman muun suorituksen
kanssa. Myˆs undefine-tyyppist‰ tiedostoa voi k‰sitell‰ asynkronisesti.
\end{huomautus}

\begin{lause} 
Olkoon $G^A$ er‰s graafi ja $S$ er‰s sen solmujen
joukko. Muodostetaan $S$:n yhten‰isist‰ komponenteista tekij‰graafi
$\bar G^A$ edell‰ selostetulla tavalla. Olkoon $\bar S$ 
tekij‰graafin solmujen joukko, joka koostuu $S$:n yhten‰isi‰
komponentteja vastaavista solmuista $P_1,P_2,...$  Oletetaan, ett‰
niiden solmujen numerointi, jotka eiv‰t kuulu joukkoihin $S$ tai
$\bar S$ on kummassakin graafissa sama.
Silloin
$\Reach(i,S)=\Reach(i,\bar S)$, miss‰ edellinen otetaan graafissa
$G^A$ ja j‰lkimm‰inen tekij‰graafissa $\bar G^A$. 
\end{lause}


Uusien nollasta eroavien alkioiden m‰‰r‰ kierroksella $i$ on
korkeintaan eliminoitavan solmun $i$ naapureiden lukum‰‰r‰
eli solmun $i$ asteluku eliminointigraafissa juuri ennen kuin
se poistetaan. T‰st‰ syyst‰ on luonnollista k‰ytt‰‰ poistettavan
solmun valinnassa ahnetta menetelm‰‰: eliminoidaan aina se solmu,
jonka asteluku on pienin. T‰m‰ on antanut k‰yt‰nnˆss‰ hyvi‰
tuloksia.

\begin{lause} Kuvassa~\ref{kuva-2}
on suoritettu esimerkin~\ref{symfac-esim-1} matriisin
eliminointi kahdella tavalla. Ylemm‰ll‰ rivill‰ eliminointi suoritetaan
ahneella menetelm‰ll‰, jolloin saadaan kaksi uutta tiet‰. Alemmalla rivill‰
suoritetaan eliminointi mahdollisimman huonolla tavalla, jolloin saadaan
kuusi uutta tiet‰!
\end{lause}


\begin{figure}
 \begin{center}
 {\tt\Huge :-)}
 \end{center}
 \caption{Hymiˆ on suosittu symboli s‰hkˆposteissa} \label{kuva-2}
\end{figure}


\newpage

\addcontentsline{toc}{section}{Viitteet}
\begin{thebibliography}{88}

\bibitem{Counihan} M.~Counihan, ''Fortran 90'', Pitman, London, 1991


\bibitem{CSC90} J.~Haataja, J.~Rahola \& J.~Ruokolainen, ''Fortran 90/95''
(2. uudistettu painos), CSC-Tieteellinen laskenta Oy, Espoo, 1998

\bibitem{Foley} J.~D.~Foley, ''Computer graphics: principles and practice'',
  Addison--Wesley, 1996.

\bibitem{Rabin} M.~Rabin, Probabilistic algorithms for testing primality,
  {\em J. Number Theory}, {\bf 12}, 128--138, 1980.

\bibitem{Rossi} T.~Rossi ja J.~Toivanen, A Nonstandard Cyclic Reduction Method,
 its Variants and Stability , 
 {\em SIAM J. Matrix Anal. Appl.}, {\bf 20}, 628--645, 1999.


\bibitem{Weber} D.~Weber, An implementation of the general number field
  sieve to compute discrete logarithms mod $p$, kirjassa ''Advances
  in Cryptology - Proc.\ Eurocrypt '95'', Springer-Verlag, 95--105, 1995.

\end{thebibliography}




\end{document}







